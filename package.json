{
  "name": "dsa-enforcer",
  "private": true,
  "version": "0.0.0",
  "description": "A timed C++ DSA practice tracker. Static site, local-only progress, hostable on GitHub Pages.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "import-csv": "node scripts/import-csv.mjs",
    "check-solutions": "node scripts/check-solutions.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
